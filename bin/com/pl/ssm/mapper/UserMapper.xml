<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- <mapper namespace="com.pl.ssm.mapper.userMapper"> -->
<mapper namespace="com.pl.ssm.mapper.UserMapper">
  <select id="findUserById" parameterType="int" resultType="com.pl.ssm.pojo.User">
    select * from carddb where cid = #{cid}
  </select>
  
  <insert id="insertUser" parameterType="user">
  <selectKey keyProperty="cid" order="AFTER" resultType="int">
  select Last_Insert_ID()
  </selectKey> 
  insert into carddb(name,sex,cardDate,deposit) values(#{name},#{sex},#{cardDate},#{deposit})
  </insert> 
  
  <delete id="deleteUser" parameterType="int">
  delete from carddb where cid=#{cid}
  </delete>
  
  <select id="findUserByName" parameterType="java.lang.String" resultType="com.pl.ssm.pojo.User">
  select * from carddb where name like '%${value}%'
  </select>
</mapper>